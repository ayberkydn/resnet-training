# @package _global_
defaults:
  - datamodule: cifar10
  - model: yuvresnet

name: yuv_resnet
project_root: /home/ayb/resnet-training

trainer:
  gpus: [1]

hparams:
  lr: 0.01
  lr_scheduler:
    factor: 0.5
    patience: 10
  early_stopping_patience: 30

